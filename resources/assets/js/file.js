// ES2015 code here!

var names = ['john', 'jack'];

names.forEach(names => alert(name));

class MyClass {

    constructor(name) {
        this.name = name;
    }

    getName() {
        return this.name;
    }
}